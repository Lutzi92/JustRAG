data/
uv.lock
cache/
lib/__pycache__
.env
.env.*
!.env.example
!.env.docker
node_modules
dist
uploads
*.tsbuildinfo
build-info.json
playwright-report/
test-results/
e2e/.auth/
.vscode
# docs/ is ignored except the files referenced from CLAUDE.md
/docs/*
!/docs/retrieval.md
!/docs/agent-orchestration.md
!/docs/feature-recipes.md
!/docs/presentation/
/docs/presentation/*
!/docs/presentation/tuning-knobs.md
!/docs/observability/
/docs/observability/*
!/docs/observability/docling.md
!/docs/runbooks/
/docs/runbooks/*
!/docs/runbooks/hnsw-reindex.md
!/docs/runbooks/release.md
# Both are documented in CLAUDE.md and migration-rollback.md is linked from
# release.md's rollback caveat — the reference an operator reaches under
# pressure. Untracked they are a dead link for anyone who clones.
!/docs/runbooks/migration-rollback.md
!/docs/runbooks/stuck-quick-jobs.md
!/docs/runbooks/spreadsheet-ingest-ops.md
.playwright-mcp
.tmp/
/example/

# OS files
.DS_Store
Thumbs.db

# Editor files
.idea/
*.sw?

# Logs
*.log

# Security - private keys/certs
*.pem
*.key
*.cert
*.p12
*.pfx

# Claude Code local files
.claude/

# Git worktrees
.worktrees/

go-backend/worker
go-backend/server
go-backend/migrate
go-backend/eval

# Same binaries when built in place (`cd go-backend && go build ./cmd/eval`
# drops the binary next to main.go). NOT a wildcard: cmd/server/default.pgo is
# meant to be committed for -pgo=auto.
go-backend/cmd/eval/eval
go-backend/cmd/eval-gen/eval-gen
go-backend/cmd/eval-genset/eval-genset
go-backend/cmd/server/server
go-backend/cmd/worker/worker
go-backend/cmd/migrate/migrate

# Eval golden data — JLU-internal, not privacy-cleared for public commit.
# See eval/golden/README.md §Sampling methodology #7 for the privacy escape hatch.
/eval/golden/production-dryrun.jsonl
/eval/golden/production-q032fix.jsonl
/eval/golden/production-ppm-2026-08.jsonl
/eval/golden/production.jsonl
/eval/golden/multi-turn-de.jsonl
# Wave 3 Task 4: global-synthesis set. Derived from the JLU Confluence corpus
# (question text names real internal projects), so it stays local like the
# other production-derived sets. The acceptance record next to it IS committed.
/eval/golden/global-synthesis-de.jsonl
/eval/golden/*.xlsx
/eval/golden/*.zip
/eval/golden/snapshots/*.json
go-backend/eval-report.json
eval/golden/multi-hop.jsonl
eval/golden/production.jsonl.stale-backup
eval/golden/multi-hop.jsonl
eval/golden/multi-kb.jsonl

go-backend/eval-gen
eval/golden/kg-community-global.jsonl
.superpowers/sdd/

# Spreadsheet-fixture golden set (Task 9, spreadsheet-ingest Phase 4): the
# seeding script (eval/fixtures/seed-spreadsheets.sh) writes a KB-id-resolved
# copy here — the committed spreadsheets-de.jsonl keeps the
# REPLACE_WITH_FIXTURE_KB_ID placeholder so it stays instance-independent.
eval/golden/*.local.jsonl
eval/golden/spreadsheets-de.report.json
